import React from 'react';

class InfoHead extends React.Component{
    render() {
            return (
                <div>
                    {this.props.children}
                </div>
            )
        }
    };

export default InfoHead;
